import 'package:flutter/material.dart';

class MemberDataScreen extends StatelessWidget {
  const MemberDataScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Member Data')),
    );
  }
}
